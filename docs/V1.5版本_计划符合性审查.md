# V1.5 计划符合性审查

审查依据：用户最后确认的《V1.5 船舱结构识别版：最终合并实施计划》，以及补充的 GPT 运行分析。截至 2026-09-21，代码检查点为 `d10ab0e`，结论是 **尚未完成全部计划，不能签发 IMPLEMENTATION_SHA 或 V1.5 PASS**。测试产物由提交前相同源码的开发工作树生成，仍需在最终精确 SHA 下复跑。

## 补充分析的处理

| 分析意见 | 核对结果与处理 |
|---|---|
| 本地提交没有出现在 GitHub | 属实。已有五个提交已推送，远端核验为 `adaf8ae9e6436ea573348dcece8c8b4fdc6444a1`；这是阶段版本，不是完整交付。main 未改变。 |
| 双路径 observed 证据偏离原规格 | 对早期规格成立，但最后合并计划已明确替换此限制，允许可见结构面与顶视表面转折两条路径。保留两条路径，分别记录机制、opening side 和 observed/inferred 标志。 |
| 需要厚度与可见性的成对反例 | 采纳。增加 0/20/40 cm × INNER/OUTER/BOTH/BLIND；同时修正原 OUTER_ONLY 等场景中仍泄漏内缘位置的密集顶部/近边舱底采样。 |
| 先取证再改 Deck 分数 | 采纳。九份 Development 的全部候选、淘汰原因、top-2 和分量已输出；未计算量为 null，不编造独立 Deck GT。没有修改评分权重或 7/9、6/8 门槛。 |
| PCD 补零例外绑定 manifest | 采纳。六个已审计文件 hash 分别绑定精确点载荷和尾零长度；相同布局、相同补零长度但不同内容的文件仍拒绝。 |
| 不能把 Synthetic 通过写成真实通过 | 采纳。新增独立交付审查工具；当前 Development 为 FAIL，完整覆盖与正式验证未完成。 |

## 按计划逐项核查

| 契约 | 当前实现/证据 | 状态 |
|---|---|---|
| 固定 BASE、功能分支、冻结 V1.4 | BASE 为 `d586b93fbf676e585e77286b7b10d7076a64d3b7`；冻结检查从 Git 基线直接比较 72 个文件，不使用可改写的分支 hash 清单 | 符合 |
| 只读 270 文件 | 实施前后逐文件 SHA-256 与文件数一致 | 符合 |
| 13/12/23/11、46 标注副本、08-08 编码别名 | manifest 及此前文件法证；新增 manifest 重建一致性检查 | 符合 |
| holdout 分组及身份不确定性 | SEALED_WEAK_HOLDOUT、golden_candidate、LIKELY_DIFFERENT、physical_vessel_id=UNKNOWN | 符合 |
| Windows strict decoder | binary、float32 XYZ、明确字段偏移、重复 padding、拒绝未知/损坏/非有限值；补零按精确文件白名单 | 内部验证通过；PCL 对照待 Ubuntu |
| Geometry Core 零 PCL、私有 kNN | 独立 Eigen 核心及 small_gicp 私有搜索；PCL I/O 和 V1.4 适配在独立 target | 符合 |
| GT 与产品类型隔离 | 产品仅接收 XYZ/ShipFrameCloud；标注在 evaluator；固定 runtime 改评分通道、shuffle 和作弊 producer 反例 | 已验证 |
| Offline 最终 Z=refined Deck normal | 两类输入同一路径；额外核查 Deck 支撑跨度、长轴退化；旋转合成验证 | 数学路径符合；真实 Deck 选择仍失败 |
| ROI/Deck 不因最高、最多或文件名选面 | 多平面/normal/连通域、必要 enclosure 及评分；全部候选诊断不读取标注 | 尚不足：真实 up/Deck/Cargo 竞争仍会误选 |
| 两条三维证据路径 | 原始 3D 面拟合与横断面平台/下降拟合；L0 只提候选 | 已实现，物理开发矩阵通过 |
| opening-side、厚度与顶视盲区 | 内外侧探测、无假设厚度位移、冲突降为 unresolved；新增成对场景 | 已验证的场景通过 |
| 缺点不等于 FOV 外/自由空间 | UNKNOWN 区域与外界的连通性只决定截断，不产生低处返回或 observed 边；封闭空洞/外连缺口反例 | 已验证 |
| 非矩形、多 Hatch、Beam | 非正交四边形、五边形、凹形开口、多 Hatch、下方有返回的跨舱梁、实体隔断、动态线状簇 | 开发矩阵已覆盖；全计划覆盖仍需最终审计 |
| 最多一条完整缺边推断 | 两端独立实测角部片段及相邻实测边共同约束时才推断一条缺失跨度；一个角、两条缺边仍为 PARTIAL | 有正反例 |
| 端点 bounded/null、支持区间 | 实测交点才 bounded；无终止证据 null；合并保留缺口；弱评估也改为实际支持区间 | 已验证 |
| normal 不确定性代理 | 可见面使用实测 normal 角度 MAD 与残差/跨度最大值；顶视线段仍主要使用残差/跨度代理 | 部分完成，不能称 calibrated covariance |
| 弱标注一对一、局部重复/歧义 | 原始 XY、多边形 1.5 m 局部域、PARTIAL 片段重复、top-2、版本间 disagreement；未标区域不算 FP | 已实现并有反例 |
| Development operational | 2026-09-21 当前代码九份 Development：Frame/Deck/联合 6/9（要求各 7/9）；非歧义一致关联 0/8（要求 6/8）；非 Deck primitive 非空 3/9 | **FAIL，版本阻塞** |
| Synthetic 15 cm、覆盖、额外结构、负控制 | 43 个物理开发场景 × 3 seeds；Deck R95 来自 GT visible support；不可观测边仍检查 Deck | 开发矩阵 129/129；报告标记未覆盖完整 V1.5 验收 |
| 反作弊 | 强制矩形、全 PARTIAL、全 inferred、重复、短准确片段、非法模型/SE(3)、GT 通道反例 | 已实现反例通过 |
| V1.4 固定 B 集成 | prepare_observation + 有效估计累积；动态点保留；原 B 不重新初始化；两后端全矩阵 | 旧 200 帧开发矩阵 GICP 合格、VGICP 不合格；当前 50 帧 Quick 动态场景 GICP 有效率 48/49，Quick FAIL；最终精确 SHA Full 待完成 |
| Quick/Full 分级 | 统一 Quick 已运行 7 个 Synthetic、两份 Development smoke、双场景双后端 50 帧 | Synthetic PASS；真实 smoke 1/2，集成无合格后端；**Quick FAIL** |
| 封存集访问与污染 | 持久化统一 ledger、文件审计与评分分离；配置冻结检查和全 13/23 入口已补；门禁不满足则不读取 holdout 做算法评分 | 机制已实现；四份评分尚未执行 |
| JSON/CSV/PCD/追溯 | 模型/Deck/polygon/primitive/报告及 CSV、彩色原始点云；产品与评分追溯分离；unknown/null 区分 | 开发产物审计通过，颜色为证据邻近显示而非点语义 GT |
| Windows 构建与旧门禁 | PCL-OFF 全量构建；排除独立 Quick 的 CTest 24/24，包括原 G1～G7 与 V1.4 安全门禁 | 开发验证通过；最终 SHA 全新目录待完成 |
| 中文文档、统一代码审查、Ubuntu 正式验证 | 文档明确当前失败与待验证；不制造正式交接 SHA、正式报告或 baseline | 尚未进入正式交付 |

## Deck 取证结论的边界

`audit_v15_deck_candidates.py` 已记录九份 Development 的全部候选，而不是只打印胜出的平面。它区分 normal 模式不支持、支撑/包围不足、没有闭合低处返回、进入排序等原因。物理分类默认为 UNKNOWN；评分器的矩形 weak annotation 不能提供真实 Deck 平面标签。

例如旧诊断中的 `4_16` 同一物理平面正法向被边界过滤、反法向获胜，说明“resolved=true”不能当作法向正确的证据。新增 UNKNOWN 连通性修复解决的是内部空洞被误判截断，并未解决全部 Cargo、外部背景、Deck 符号与高度竞争。不可由当前表格宣称真实 Deck 已正确。

## 仍需完成的工作

1. 修正真实 Deck/ROI/up 选择与真实边界证据，使九份 Development 在原门槛下通过；不能用粗 bbox 或改关联门槛掩盖失败。
2. 解决当前 Quick 的真实 smoke 和动态集成失败；保持 99% 正式有效率要求和失败帧原始证据。
3. 核查完整 Synthetic 必测覆盖、顶视表面转折的不确定性代理与每一项配置的实际作用；当前 129/129 是开发子集。
4. 固定完整代码与配置后在最终 SHA 全新目录运行 Windows 全矩阵；只有前置门禁通过才统一评分四份 holdout。
5. 完成统一代码审查和 Ubuntu20 PCL-ON clean build、Full 与 decoder 对照，再按用户流程决定是否合并。

当前状态为 **继续修复，未达到“代码实现完成，等待统一代码审查”**。不合并 main，不创建 V1.5 基线，不进入 V1.6。`REAL_FORMAL_15CM=PENDING_GOLDEN`，`SITE_ACCURACY=SITE_PENDING`。
