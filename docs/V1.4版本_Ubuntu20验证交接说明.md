# V1.4 Ubuntu20 统一验证交接

## 当前边界

先完成统一代码审查，再由 Ubuntu20 ClaudeCLI 验证用户收到的精确 `IMPLEMENTATION_SHA`。本功能分支不合并 main、不创建 V1.4 基线标签、不进入 V1.5。

针对 `02c7926ca0d06fdfd77387a4b483f0f1b6bb0e05` 的审查要求已纳入集中修复：稳定/晋升点持续接受隔离审核、PCL 基础有效性门禁、初始化体素质心及顺序不变性。**Ubuntu20 正式测试仍暂缓，先把修复后精确 SHA 交 GPT 快速复审；复审通过后才执行下列命令。**

`BASE_SHA=06af2d66262afd828edf2eef72b6ce1bc6969797` 是用户指定开发起点；它不代替原始 M0 正式通过报告。当前 `ORIGINAL_M0_EVIDENCE=PENDING_ARCHIVE`，不得补造证据。

## 精确 SHA 与全新构建

现有工作树有未知修改时先保存用户文件，不能用 reset/clean 强行清理。把交付消息中的完整 40 位 SHA 填入下列命令；不以分支名替代验证目标。

```bash
cd /你的路径/Ship-Unloader
SHA='<IMPLEMENTATION_SHA 的完整 40 位值>'
test -z "$(git status --porcelain)" || exit 1
git fetch origin feature/v1-4-ship-track-map
git checkout --detach "$SHA"
test "$(git rev-parse HEAD)" = "$SHA" || exit 1
test -z "$(git status --porcelain)" || exit 1
bash ship_perception/scripts/validate_ubuntu20.sh \
  --sha "$SHA" \
  --base-sha 06af2d66262afd828edf2eef72b6ce1bc6969797 \
  --cmake /usr/bin/cmake --ctest /usr/bin/ctest --jobs 2
```

首次获取可用 `git clone git@github.com:guolichen007/Ship-Unloader.git`。系统依赖安装命令见[构建说明](../ship_perception/README.md)。验证要求 Ubuntu20、GCC9、CMake3.16、Eigen3.3.7、PCL-ON；记录 PCL 实际版本与头文件路径。默认优先系统 Eigen 配置，不能以 pkg-config 的另一个版本代替实际编译依赖证据。

脚本每次新建 `validation/<SHA>/<UTC-run-id>/build`，不删除历史记录或复用缓存，构建本身离线。按顺序执行验收器反例、生成器/PCD 自检、真实后端、地图安全、原 G5 harness、G1～G7 Quick，然后运行 **Full Acceptance**。PCL GICP 的已知 SE(3) 必须执行、收敛、有效并达到 5 cm/0.5°；缺一项即失败。每场景/种子首对帧对照须有完整 pose/quality 记录、真实后端调用且没有数学非法输出，其完整矩阵精度不承担版本放行。

Full 为两后端 × 17 场景 × 3 种子 × 200 帧；闭环与相邻帧 benchmark 独立评分，初始化帧单列。场景过滤或 Quick 结果不能替代 Full。至少同一个后端贯穿全部 11 个正常场景及种子达标。

## 必须审查的证据

- 构建日志、CTest 清单及日志、实际依赖文件，精确 SHA 和干净工作树审计。
- `v14_full/report.json` 的两个后端比较、合格集合、编译 SHA、配置哈希与产物校验值。
- `pose.csv` 中无效帧、末帧误差、每帧静态 P95 与样本数，不能仅看总体均值。
- `quality.csv` 中真实执行、收敛、失败原因、数学检查、共同质量、H 可用性及公平请求指纹。
- `map.csv` 中帧开始目标 revision、提交后地图和候选 revision；拒绝帧不得污染状态。
- `timing.csv` 的补偿/配准/地图/总耗时；开发机数值不能承诺现场实时性能。
- 捕获范围、坐标自由度诊断、参考地图及抽样累积观测 PCD；后者含动态点。
- GT 隔离、非法输出、合法错误位姿、候选污染、首帧暂态隔离、遮挡、容量及参考支撑不足安全断言。
- 已稳定 bootstrap 点和已晋升点都要覆盖：支持≥5帧→明确冲突→SUSPECT→QUARANTINED→从快照移除→重新完整晋升恢复→再次可隔离。未观察到或遮挡不能计冲突。
- 初始化质心数值、原顺序与 shuffle 顺序的地图一致性，含容量截断场景。

主要日志在运行目录内，统一结构化报告为 `report.json`，中文摘要为 `验证摘要.md`。原始产物保留在 Ubuntu；正式报告归档时记录路径及哈希，不把报告脚本的建议判定直接当成独立结论。

ClaudeCLI 除检查 CSV，还必须随机抽查 `NORMAL_6DOF`、`TRANSIENT`、`INDEPENDENT_VIEW_SAMPLING`、`FLAT_DECK` 四类场景的 `*_tracking_reference.pcd` 与 `*_accumulated_stride10.pcd`。每类至少抽一个种子、两个后端，记录抽样文件名/哈希、可视化截图和双影/残影/错层观察结论。累积文件含动态点，需区分动态轨迹与静态结构重影；没有实际看图就记为未完成，不能从 CSV 推断视觉通过。轻量 ZIP 未包含大型 PCD 时，使用本机正式运行生成的原始 PCD。

`PRIMARY_CANDIDATE=GICP`、`SECONDARY_CANDIDATE=VGICP`；最终生产后端未冻结。

## ClaudeCLI 返回格式

```text
VALIDATION_SHA=
BASE_SHA=06af2d66262afd828edf2eef72b6ce1bc6969797
BRANCH=DETACHED
WORKTREE=
OS=Ubuntu 20.04
GCC= / GXX= / CMAKE=
EIGEN_VERSION= / EIGEN_CONFIG_DIR= / EIGEN_INCLUDE_DIRS=
PCL_VERSION= / PCL_CONFIG_DIR= / PCL_INCLUDE_DIRS=
SMALL_GICP_COMMIT=fd29d8cf94cf05ed7ad21c81c27b65963110adb5
CONFIG_HASH=
BUILD_GATE=
G1= / G2= / G3= / G4= / G5= / G6= / G7=
G5_HARNESS=
V14_QUICK=
V14_FULL=
GICP_ALL_REQUIRED_SCENES=
VGICP_ALL_REQUIRED_SCENES=
QUALIFIED_BACKENDS=
MAP_POLLUTION_TESTS=
GT_ISOLATION=
STABLE_REFERENCE_REQUARANTINE=
INITIALIZATION_ORDER_INVARIANCE=
PCL_KNOWN_SE3_GATE=
PCL_COMPARISON_EVIDENCE=
PCD_VISUAL_SPOTCHECK=（抽样清单、截图、观察结论）
FIRST_FAIL= / FAIL_TYPE=
PRODUCT_CODE_MODIFIED_BY_CLAUDE=NO
CLAUDE_PATCH_SHA=NONE
LOG_DIR= / ARTIFACT_DIR=
ORIGINAL_M0_EVIDENCE=PENDING_ARCHIVE（或真实已归档证据引用）
V14_CODE= / V14_SYNTHETIC= / V14_LINUX20_BUILD=
V14_SITE=SITE_PENDING
FINAL_DECISION=
ALLOW_MERGE=NO（独立报告提交后由用户审查决定）
```

CODE_FAIL 返回 Codex 修复并产生新 SHA；Ubuntu 对新 SHA 重新 clean build 和 Full。TEST_FAIL 先复核测试设计，不能删失败样本或降低门槛。ENV_FAIL 保留环境证据，保持 Ubuntu20 目标。现场缺失持续 SITE_PENDING。

只有统一代码审查、同一 SHA 的正式验证均通过且报告归档后，才可由用户决定合并 main 并固化 V1.4 基线；本次集中修复交付停在“等待 GPT 复审”。
