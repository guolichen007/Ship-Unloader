# V1.5-R1-S1 局部甲板恢复基线复盘

## 范围与复现

- 基线源码：`169e992ddb8e3243ebd5522ab4a791425f31c1a2`；R1-S1 执行源码：`05a6402dec689bdb75a6dcec2dd4cf85e466240a`。
- 分支：`feature/v1-5r1-local-deck-recovery`。
- 输入：manifest 中的六个 Development PCD；两轮对应场景的输入 SHA256 和配置哈希逐一相同，无 override。
- 输出：`Ship-Unloader-Work/r1_static/baseline_001` 与 `Ship-Unloader-Work/r1_static/baseline_002`。这些运行产物不进入 Git。
- 命令：`python -m ship_perception.r1_static.batch --scene-set normal6 --run-id baseline_002`。

首次运行在 6-8、8-22 生成 Opening 后，因空格网单元参与 `mean_drop_m` 均值而写出 NaN；该运行原样留在 `baseline_002_serialization_failure`。修复仅令均值使用有真实回波的单元，没有修改 Opening 判定阈值。正式 `baseline_002` 的六场景结果均完整写出。

## 六场景结果

| 场景 | Proposal | baseline_001 Local Deck resolved | baseline_002 Local Deck resolved | baseline_002 Opening | 最终算法确认 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 08-01 | 5 | 0 | 0 | 0 | 0 |
| 08-08 | 4 | 0 | 0 | 0 | 0 |
| 4-16 | 1 | 0 | 0 | 0 | 0 |
| 6-8 | 3 | 0 | 1 | 2 | 0 |
| 7-16 | 1 | 0 | 0 | 0 | 0 |
| 8-22 | 5 | 0 | 1 | 1 | 1 |
| **合计** | **19** | **0** | **2** | **3** | **1** |

6-8 的两个 Opening 均为 `UNRESOLVED`；8-22 的一个 Opening 为 `PARTIAL`。最后一列仅为算法输出，尚未经 CloudCompare 人工位置、边界或舱数核验，不能作为静态船舱验收通过。

baseline_001 的 19 个 Local Deck 失败为竞争平面 10、连通支撑不足 8、扇区不足 1。baseline_002 的 17 个失败为竞争平面 15、单侧支撑 2；连通支撑不足 0、扇区不足 0。新的竞争平面计数属于新判据的结果，不能仅凭数量变化判定误报或真实多平面，需结合每个 Proposal 的候选面锚点高度、法向夹角和独立支撑复核。

## 法证与限制

19 个 Proposal 均包含 `raw_ring_point_count`、`plane_inlier_count`、`ransac_hypothesis_count`、`plane_cluster_count`、候选面支撑数/锚点高度/法向夹角、连通分量数/大小、接受的分量编号、扇区数和拒绝阶段。`candidate_plane_support_counts` 是最多 3000 点 RANSAC 样本的计数；`plane_inlier_count` 是初始最佳平面的原始 ring 内点数，后续重新拟合后分量总数可能不同。

物理连通采用配置中现有的 `roi.support_connectivity_m=1.0`，允许同一拟合平面上的多个独立分量联合支撑；Proposal 保留跨尺度原始种子单元，Local Deck 依真实种子几何选择邻域。未修改 `v15.json`、GT 隔离、Profile、3D Face、Hypothesis 或验收阈值。

本轮第一门“真实 Proposal 进入 Opening”已通过。真实边界精度、Proposal 位置质量及 8-22 的最终舱数仍未验收；继续优化前应人工核验这三个 Opening 及剩余竞争平面法证。暂不进行 Ubuntu 正式精度验收、C++ 移植、merge 或 tag。
